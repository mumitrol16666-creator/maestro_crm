#!/usr/bin/expect -f

set timeout 30
set server "38.180.206.183"
set user "root"
set password "LMQ2kUkBBR"

spawn ssh ${user}@${server}
expect {
    "password:" {
        send "${password}\r"
    }
    "yes/no" {
        send "yes\r"
        expect "password:"
        send "${password}\r"
    }
}

expect "# "
send "echo \"=== Структура nginx конфигурации ===\"\r"
expect "# "
send "cat /etc/nginx/sites-enabled/lgardens-ip | head -50\r"
expect "# "
send "exit\r"
expect eof
