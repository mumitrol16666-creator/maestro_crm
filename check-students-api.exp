#!/usr/bin/expect -f

set timeout 30
set password "D7V7vcfK2i"
set host "root@149.33.0.114"

spawn ssh -o StrictHostKeyChecking=no $host
expect "password:"
send "$password\r"
expect "#"

send "curl -v http://localhost:5000/api/students\r"
expect "#"

send "exit\r"
expect eof
