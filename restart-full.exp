#!/usr/bin/expect -f

set timeout 30
spawn ssh root@149.33.0.114 "systemctl reload nginx && pm2 restart all"
expect {
    "password:" { send "D7V7vcfK2i\r" }
    "yes/no" { send "yes\r"; exp_continue }
}
expect eof
